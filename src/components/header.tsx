import styles from "../styles/index.module.scss"

export default function Header() {
    return (
        <header className={styles.header}>
            <h1>
                Bem vindo ao Dev Chat
            </h1>
        </header>
    )
}